require('dotenv').config()

const token = 'Tu Token';

module.exports = {
  prefix: '!',
  BOT_TOKEN: token,
  statusBOT: ['mi prefix !', 'Talleres', 'a SendTech', 'Tiktoks'],
  embedColor: 0xf75762, 
}
